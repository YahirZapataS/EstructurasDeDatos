#include <iostream>

using namespace std;

void imprimir (int * arr, int n) {
    for (int i = 0; i < n; i++) {
        cout << arr[i] << " ";
    }
}

void heapSort (int * arr, int n) {
    for (int = n / 2 - 1)
}

int main () {
    int arr[] = {11, 22, 25, 5, 14, 17, 2, 18};
    int n = sizeof(arr) / sizeof(arr[0]);

    cout << "Arreglo original" << endl;
    imprimir(arr, n);

}